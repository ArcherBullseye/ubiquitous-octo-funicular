version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: archerbullseye-solar-miner_server_1
      APP_PORT: 3000

  server:
    build: https://github.com/ArcherBullseye/solis-luxos-controller.git
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
    restart: unless-stopped
